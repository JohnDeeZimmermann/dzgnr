# Two Page Card Example

- Artifact: front/back business card
- Dimensions: 9.0 x 5.5 cm
- Mode: `combined` (single two-page PDF)
- Assets: `assets/mark.svg` local vector accent

Render from this directory:

```bash
dzgnr render front.html --config dzgnr.json
```
